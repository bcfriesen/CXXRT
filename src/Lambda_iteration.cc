#include "ray.hh"
#include "globals.hh"

void Lambda_iteration(const double lambda) {

/*
 *    for (Ray& r: rays) {
 *      for (RayData& rd: r.raydata) {
 *        rd.calc_source_fn(lambda);
 *      }
 *      r.formal_soln(lambda);
 *    }
 *
 *    for (GridVoxel& gv: grid) {
 *      for (WavelengthPoint& wlp: gv.wavelength_grid) {
 *          wlp.calc_J();
 *      }
 *    }
 */

}
